version: 2.1
setup: true
orbs:
  path-filtering: circleci/path-filtering@3.0.0
  toolkit: digital-prstv/circleci-toolkit@4.0.2
parameters:
  success_flag:
    type: boolean
    default: false
    description: |
      If set to true, the success workflow will be triggered.
  validation_flag:
    type: boolean
    default: false
    description: |
      If set to true, the generate-config workflow will be triggered.
  min_rust_version:
    type: string
    default: "1.81"
  fingerprint:
    type: string
    default: SHA256:OkxsH8Z6Iim6WDJBaII9eTT9aaO1f3eDc6IpsgYYPVg

workflows:
  check_last_commit:
    when:
      and:
        - not: << pipeline.parameters.success_flag >>
        - not: << pipeline.parameters.validation_flag >>

    jobs:
      - toolkit/choose_pipeline:
          name: choose pipeline based on committer
          context: bot-check

  generate-config:
    when:
      and:
        - not: << pipeline.parameters.success_flag >>
        - << pipeline.parameters.validation_flag >>

    jobs:
      - path-filtering/filter:
          base-revision: main
          config-path: .circleci/continue_config.yml
          mapping: |
            content/.* update-website true
            sass/.* update-website true
            static/.* update-website true
            templates/.* update-website true
            syntaxes/.* update-website true
            themes/.* update-website true
            config.toml update-website true
            .circleci/.* update-changelog true
            justfile update-changelog true
            README.md update-changelog true
            renovate.json update-changelog true
            .githooks/.* update-changelog true

  on_success:
    when:
      and:
        - << pipeline.parameters.success_flag >>
        - not: << pipeline.parameters.validation_flag >>

    jobs:
      - toolkit/end_success
